import styles from "./RegisterUser.module.css";

const RegisterUser = (props) => {
  const classes = `${props.className}`;

  return (
    <p className={classes}>
      Register yourself on contact application created by Ahmad Raza
    </p>
  );
};

export default RegisterUser;
